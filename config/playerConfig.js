export const playerConfig = {
    radius: 15,
    speed: 300, // Pixels per second (was 5 per frame)
    color: 'blue',
    fireRate: 200, // 射击间隔 (毫秒)
    bulletSpeed: 600, // Pixels per second (was 10 per frame)
    strength: 10,
    agility: 10,
    comprehension: 10,
    defense: 0,
    initialReiki: 9999999,
    maxHp: 10
};